# Enigma-Emulator
Emulator of Enigma in Python. Encryption very slimmed down though.

This is designed to run in Python 3.6. Older versions of 3.x may work, but 2.7 will not. 
Run in PyCharm, CMD, Terminal, anywhere where python can be run
